version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - graphviz
  jobs:
    pre_build:
      - make -C doc autoxml.rst

sphinx:
  configuration: doc/conf.py
  fail_on_warning: true

python:
  install:
  - requirements: ci/requirements.txt
