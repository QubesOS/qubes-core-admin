version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - graphviz
  jobs:
    pre_build:
      - make -C doc autoxml.rst

sphinx:
  configuration: doc/conf.py

python:
  install:
  - requirements: ci/requirements.txt
